version: '3.8'

services:
  postgres:
    image: postgres:16-alpine
    container_name: postgres-catalogo-dev
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: kuki30222002
      POSTGRES_DB: db-catalogo
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: